# Copy this to .env (gitignored) before running docker compose up.
# Generate a real secret with, e.g.:
#   PowerShell: [Convert]::ToBase64String((1..48 | ForEach-Object { Get-Random -Max 256 }))
AUTH_JWT_SECRET=replace-with-a-real-random-secret-at-least-32-bytes
